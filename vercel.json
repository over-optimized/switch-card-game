{
  "buildCommand": "pnpm --filter switch-shared run build:prod && pnpm --filter switch-client run build:prod",
  "outputDirectory": "client/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "ignoreCommand": "bash -c 'COMMIT_MSG=\"$VERCEL_GIT_COMMIT_MESSAGE\"; echo \"Commit: $COMMIT_MSG\"; if [[ \"$COMMIT_MSG\" == *\"server:\"* ]]; then echo \"🚫 Skipping Vercel build - server-only commit\"; exit 1; else echo \"✅ Deploying to Vercel - client/shared changes or general commit\"; exit 0; fi'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}