{
  "buildCommand": "pnpm --filter switch-shared run build:prod && pnpm --filter switch-client run build:prod && cp -r ./dist ../dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "ignoreCommand": "bash -c 'if [[ \"$VERCEL_GIT_COMMIT_MESSAGE\" == *\"server:\"* ]] || [[ \"$VERCEL_GIT_COMMIT_MESSAGE\" == *\"feat: implement Railway\"* ]] || git diff HEAD^ HEAD --name-only | grep -E \"^(server/|railway\\.toml|docs/.*\\.md)$\" > /dev/null; then echo \"🚫 Server-only changes detected - skipping Vercel build\"; exit 1; else echo \"✅ Client changes detected - proceeding with Vercel build\"; exit 0; fi'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}